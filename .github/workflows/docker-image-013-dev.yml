name: Docker Image CI

on:
  push:
    branches: [ "blackcoin-more-0.13-dev" ]
  pull_request:
    branches: [ "blackcoin-more-0.13-dev" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
